spring:
  application:
    name: online

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1082266400914-bcehrm9vvvt4l5t0fhblvtvuueae1dke.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email

  datasource:
    url: ${POSTGRES_URL}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true
    show-sql: true

  data:
    mongodb:
      uri: ${MONGO_URI}

  redis:
    host: localhost
    port: 6379
    timeout: 6000
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0

  cache:
    type: redis


app:
  jwt:
    secret: ${SECRET_KEY}
  elasticsearch:
    url: ${ES_URI}
  aws:
    access_key_id: ${ACCESS_KEY_ID}
    secret_access_key: ${SECRET_ACCESS_KEY}
    region: ${REGION}

#logging:
#  level:
#    org.springframework.web: INFO
#    org.hibernate.SQL: INFO

